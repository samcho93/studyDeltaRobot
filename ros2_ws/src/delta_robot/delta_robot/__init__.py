"""ROS 2 nodes for the studyDeltaRobot course (driver, commander, web bridge).

The robot math lives in the ``deltarobot`` library (pip install -e <repo>/python);
ROS-independent node logic is in :mod:`delta_robot.core`.
"""
