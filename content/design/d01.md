---
id: d01
track: design
title: 웹 시뮬레이터 사용법
duration: 45
level: 초급
requires: [t03]
tools: [sim]
---

## 학습 목표

- 시뮬레이터의 다섯 탭(**설계 · 조그 · 작업 · 분석 · 연결**)이 각각 무엇을 하는지 알고, 설계 → 검증 → 수정의 흐름으로 쓸 수 있습니다.
- 분석 탭의 모터 속도 ω·토크 τ 그래프를 한계선과 비교해 읽고, "어디서 왜" 한계를 넘는지 설명할 수 있습니다.
- 설계를 URL·JSON으로 공유하고, 같은 설계를 URDF 뷰어·Python Playground에서 이어 쓸 수 있습니다.

## 화면 구성

@btn[~/sim/index.html] 시뮬레이터 열기

시뮬레이터는 설치 없이 브라우저에서 동작합니다. 가운데 3D 화면에 로봇이 보이고, 옆 패널의 다섯 탭으로 모든 기능을 씁니다.
상단에는 **비상정지** 버튼(PC Python·ROS 2와 연결되어 있으면 정지 명령을 보냄), **테마** 전환, 다른 도구(URDF 뷰어, Playground, 기구학 실험실)로 가는 링크가 있습니다.

@fig[d01-sim-tabs] 다섯 탭의 역할과 설계 반복 흐름. 설계 탭에서 값을 바꾸고 [설계 검증]과 분석 탭으로 확인한 뒤, ✖가 있으면 설계 탭으로 돌아갑니다.

| 탭 | 하는 일 | 이 트랙에서 주로 쓰는 곳 |
|---|---|---|
| **설계** | 프리셋 선택, 치수 슬라이더(R, r, L, l, w), 모터각 한계, 재질, 모터, 감속기·감속비, 툴, 페이로드, 이펙터 질량. 파생값 표(질량·관성·정적 토크)와 **[설계 검증]** 버튼 | D02~D05, D08~D11 |
| **조그** | TCP x/y/z 슬라이더(역기구학), 모터 θ1~θ3 슬라이더(순기구학). 모터각·조건수·볼조인트각·팔꿈치각·정적 토크 표시, 한계를 넘으면 빨간색 | D03, T06 |
| **작업** | 장면(빈 작업대 / 분류 / 컨베이어 / 그리기), 데모 프로그램(Adept 사이클 / 분류 데모 / 컨베이어 데모 / 원·별 그리기), 궤적 설정(프로파일·속도·가속도·아치 높이), 재생·일시정지·처음으로, 재생 배속 | D06, D08~D10 |
| **분석** | 모터각 θ·모터 속도 ω·모터축 토크 τ 그래프(한계선 표시), 검증 결과(피크/RMS/속도/관성비), 사이클 시간·분당 사이클, 작업영역 점구름·작업 실린더 표시 | D03, D04, D06 |
| **연결** | [PC Python 연결](websim 백엔드·ROS 2 web_bridge 공용), 타임라인 JSON 불러오기, 설계 JSON 내보내기/불러오기, URDF 내려받기, 공유 링크 복사 | D07, R03, R06 |

### 설계 탭 — [설계 검증]

설계 탭의 **[설계 검증]** 은 지금 설계로 두 가지를 계산해 보여 줍니다.

1. **작업 실린더 D×H** — 어느 방향으로든 도달할 수 있는 원기둥 (T07, `kinematics.work_cylinder`)
2. **표준 사이클의 모터 검증 결과** — 정해진 동작을 돌려 보고 피크 토크·RMS 토크·속도 등이 모터 한계 안에 있는지 ✔/✖

파생값 표에는 위팔 질량, 이동판 질량(이펙터 + 툴 + 페이로드 + 아래팔 절반), 팔 관성, 정적 토크가 나옵니다.
값을 하나 바꿀 때마다 이 표가 어떻게 변하는지 보는 습관이 설계 감각을 가장 빨리 키웁니다.

### 조그 탭 — 한계를 손으로 느끼기

TCP 슬라이더를 움직이면 역기구학으로 모터각이 계산되고, 모터 슬라이더를 움직이면 순기구학으로 TCP가 계산됩니다.
모터각 한계, 볼조인트 각도 한계, 팔꿈치 각도 한계(20°~165°) 중 하나라도 넘으면 해당 값이 **빨간색**이 됩니다.
작업영역 가장자리로 갈수록 **조건수**가 커지는 것도 여기서 확인합니다(T06).

### 작업 탭과 분석 탭 — 움직이고, 재고, 판정하기

작업 탭에서 장면과 데모 프로그램을 고르고 재생하면, 분석 탭에 그 동작의 그래프와 검증 결과가 나옵니다.
Playground에서 실행한 Python 프로그램의 동작도 같은 방식으로 재생·분석됩니다.

## 그래프 읽는 법

분석 탭의 그래프는 **세 모터의 곡선 + 한계선**으로 되어 있습니다. 아래 그림은 `edu_servo`(MG996R급 RC 서보)로 180 mm짜리 축소 Adept 사이클을
일부러 빠르게(0.6 m/s, 6 m/s²) 돌린 결과를 같은 방식으로 그린 것입니다.

@fig[d01-graph-reading] 위: 모터 속도 |ω|와 최고 속도선. 아래: 필요 토크 |τ|(실선), 그 순간의 속도에서 모터가 낼 수 있는 토크(점선), 피크·정격 토크선. 모두 `deltarobot.dynamics.analyze()` 결과입니다.

읽는 순서는 다음과 같습니다.

1. **속도(위)** — 곡선이 빨간 최고 속도선에 닿으면 모터가 그 속도로 돌 수 없습니다. 이 예에서는 최대 5.01 rad/s로 한계 6.0 rad/s 아래입니다.
2. **피크 토크(아래, 빨간선)** — 필요 토크의 최댓값 0.46 N·m는 피크 1.08 N·m의 절반도 안 됩니다. 여기까지만 보면 "통과"처럼 보입니다.
3. **속도-토크 곡선(아래, 점선)** — RC 서보는 빨리 돌수록 낼 수 있는 토크가 직선으로 줄어듭니다(`peak × (1 − ω/ω_max)`, D04).
   빠르게 도는 **바로 그 순간**에 필요한 토크가 점선 위로 올라가면 불합격입니다. 이 예의 검증 결과는
   `curve_ratio = 2.32`, 문제 목록은 `속도-토크 곡선 초과: 해당 속도에서 낼 수 있는 토크보다 큰 토크가 필요합니다` 입니다.
4. **정격 토크(아래, 주황선)** — 순간값이 아니라 **RMS(제곱 평균)** 와 비교합니다. 짧게 넘는 것은 괜찮지만 평균이 넘으면 모터가 뜨거워집니다.

:::warning 피크선만 보면 속습니다
"최대 토크 < 피크 토크, 최대 속도 < 최고 속도" 두 조건을 따로 만족해도, **높은 속도와 큰 토크가 동시에** 필요하면 모터는 따라가지 못합니다.
검증 결과의 속도-토크 곡선 비(`curve_ratio`)가 1을 넘는지 꼭 확인하세요.
:::

## 설계 공유와 다른 도구로 넘기기

- **URL 파라미터** — `sim/index.html?preset=edu_servo`는 프리셋으로, `?scene=conveyor`는 장면을 골라 엽니다. 둘을 함께 써도 됩니다(아래 버튼).
- **공유 링크 복사** (연결 탭) — 현재 설계 전체가 URL의 `#design=...` 부분에 담깁니다. 메신저로 보내면 받는 사람이 같은 설계를 엽니다.
- **설계 JSON 내보내기/불러오기** (연결 탭) — 보고서에 첨부하거나 PC Python에서 `DeltaRobot("my_design.json")`, `DeltaDesign.load("my_design.json")`으로 읽습니다.
- **URDF 내려받기** (연결 탭) — D07에서 자세히 다룹니다.
- **자동 공유** — 시뮬레이터에서 마지막으로 쓴 설계는 브라우저에 저장되어 **URDF 뷰어와 Playground가 같은 설계**로 열립니다.
   단 Playground 코드에 `DeltaRobot("edu_servo")`처럼 설계를 적으면 코드의 설계가 우선합니다.

@btn[~/sim/index.html?preset=industrial_picker&scene=conveyor] 예: 산업용 프리셋 + 컨베이어 장면으로 열기

## 실습 (시뮬레이터)

:::task 실습 A — 다섯 탭 한 바퀴
1. 프리셋 `edu_dynamixel`을 고르고 **[설계 검증]** 을 누릅니다. 작업 실린더 D×H와 ✔/✖를 기록합니다.
2. **조그** 탭에서 TCP x를 끝까지 밀어 처음으로 빨간색이 되는 값(모터각·볼조인트·팔꿈치 중 무엇인지)을 기록합니다.
3. **작업** 탭에서 장면 "분류", 데모 "분류 데모"를 재생합니다. 재생 배속을 바꿔 보세요.
4. **분석** 탭에서 τ 그래프의 가장 큰 봉우리가 동작의 어느 순간(들어 올림 / 가로 이동 / 내려놓음)인지 찾습니다.
5. **연결** 탭에서 공유 링크를 복사해 새 탭에 붙여 넣고, 같은 설계가 열리는지 확인합니다.
:::

:::task 실습 B — 일부러 한계 넘기기
1. 프리셋 `edu_servo`, 장면 "빈 작업대", 데모 "Adept 사이클"을 고릅니다.
2. 궤적 설정의 속도와 가속도를 조금씩 올리며 재생하고, 분석 탭 검증 결과에 처음 ✖가 뜨는 설정을 찾습니다.
3. 그때 그래프에서 속도·토크 곡선이 한계선과 어떤 관계인지 위의 "그래프 읽는 법" 순서대로 설명합니다.
:::

## 실습 (Python Playground)

같은 설계·장면을 Python으로도 움직일 수 있습니다. **Playground에서 열기**를 누르면 실행 결과가 오른쪽 내장 시뮬레이터에서 재생됩니다.

```python run
from deltarobot import DeltaRobot

robot = DeltaRobot("edu_dynamixel", scene="pick_place")
print(robot)
robot.home()
print("홈 TCP 위치 [m]:", tuple(round(v, 3) for v in robot.position))
print("부품 %d개, 상자 %d개" % (len(robot.parts()), len(robot.bins())))

# 첫 부품을 집어 같은 색 상자에 놓기
p = robot.parts()[0]
b = [b for b in robot.bins() if b["color"] == p["color"]][0]
robot.arch_to(p["x"], p["y"], p["top"])
robot.tool_on()
robot.wait(0.2)
robot.arch_to(b["x"], b["y"], robot.scene.surface_z + b["h"] + 0.02)
robot.tool_off()
print("점수:", robot.scene.score(), " 경과 시간 %.2f s" % robot.time)

res = robot.analyze()
print("피크 %.2f  RMS %.2f  속도 %.2f  ->  %s" % (
    res["peak_ratio"], res["rms_ratio"], res["speed_ratio"], "통과" if res["ok"] else res["problems"]))
```

```text
DeltaRobot(motor=dxl_xm430_w350, tool=gripper_servo, backend=RecordBackend)
홈 TCP 위치 [m]: (0.0, 0.0, -0.349)
부품 6개, 상자 3개
점수: {'per_bin': {'A': 1, 'B': 0, 'C': 0}, 'correct': 1, 'total': 1}  경과 시간 2.20 s
피크 0.16  RMS 0.33  속도 0.43  ->  통과
```

`analyze()`의 피크·RMS 비는 **(필요 토크 × 안전율 1.2) / 모터 한계**, 속도 비는 **최대 모터 속도 / 최고 속도**(안전율 없음)입니다.
1보다 작으면 통과이고, 0.16처럼 작으면 모터가 넉넉하다는 뜻입니다.

:::check 스스로 확인
- 분석 탭의 검증 결과와 Python `analyze()`가 돌려주는 `peak_ratio`·`rms_ratio`·`speed_ratio`·`inertia_ratio`는 각각 어떤 그래프·한계선과 대응하나요?
- `edu_servo` 예에서 최대 속도(5.01 rad/s)와 최대 토크(0.46 N·m)가 모두 한계 안인데 불합격인 이유를 한 문장으로 말할 수 있나요?
- Playground 코드에 설계를 적지 않고 `DeltaRobot()`만 쓰면 어떤 설계가 쓰이나요?
:::

## 자주 나는 오류와 해결

| 증상·메시지 | 원인 | 해결 |
|---|---|---|
| 조그 슬라이더 값이 빨갛게 바뀌고 로봇이 따라오지 않음 | 모터각·볼조인트·팔꿈치 한계 초과 | 조그 탭 표시에서 어떤 한계인지 보고 반대 방향으로 |
| `경고: 관절 속도 ... rad/s 가 모터 한계 ... rad/s(감속 후)를 넘습니다 — set_speed()로 속도를 낮추세요` | Playground(record)는 경고만 하고 계속 실행 | `robot.set_speed()`로 낮추고, 실물용 serial·ros2 백엔드는 이 경우 `WorkspaceError`로 거부된다는 점을 기억 |
| Playground에서 시뮬레이터와 다른 설계가 나옴 | 코드의 `DeltaRobot("프리셋")`이 우선 | 시뮬레이터 설계를 쓰려면 설계 인자를 비우거나 JSON으로 내보내 사용 |
| 공유 링크를 열었는데 기본 설계가 나옴 | `#design=` 뒤가 잘림(메신저 줄바꿈 등) | 연결 탭에서 다시 복사하거나 설계 JSON 파일로 전달 |
| `RuntimeError: 가상 시간 600 s 제한을 넘었습니다 (무한 루프인가요?)` | Playground 가상 시간 한도 초과 | 반복문 종료 조건 확인, `wait()` 누적 시간 확인 |

## 과제

1. 네 프리셋(`edu_servo`, `edu_dynamixel`, `printer_stepper`, `industrial_picker`)의 [설계 검증] 결과(D×H, ✔/✖)와 파생값 표의 이동판 질량을 한 표로 정리하세요.
2. 실습 B에서 찾은 "처음 ✖가 뜨는 설정"을 공유 링크로 만들어 제출하고, 그래프 캡처에 한계를 넘는 지점을 표시하세요.
3. Playground 예제의 `robot.parts()[0]` 대신 **모든 부품**을 같은 색 상자에 넣도록 바꾸고, 점수와 `analyze()` 결과를 보고하세요.

## 참고자료

- 이 저장소 `docs/API.md` §10 — 시뮬레이터·Playground·URDF 뷰어 화면 구성.
- Pyodide 문서 — https://pyodide.org (브라우저에서 Python을 실행하는 런타임).
- three.js 문서 — https://threejs.org/docs/ (시뮬레이터 3D 표시).
- T03 좌표계와 설계 파라미터, T07 작업영역 (이 강의).
